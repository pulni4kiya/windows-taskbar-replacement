<Window x:Class="TaskbarNicifier.App.Views.TaskbarOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskbarNicifier.App.Views"
        xmlns:vm="clr-namespace:TaskbarNicifier.App.ViewModels"
        mc:Ignorable="d"
        Title="Taskbar Nicifier"
        Height="80"
        Width="600"
        MinHeight="70"
        MinWidth="320"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        AllowsTransparency="True">
    <Window.DataContext>
        <vm:TaskbarOverlayViewModel />
    </Window.DataContext>

    <Border Background="#CC111111"
            CornerRadius="14"
            Padding="10">
        <DockPanel LastChildFill="True" VerticalAlignment="Center">
            <Button DockPanel.Dock="Left"
                    Width="34"
                    Height="34"
                    Margin="0,0,10,0"
                    Command="{Binding ToggleModeCommand}"
                    ToolTip="{Binding ModeToolTip}">
                <TextBlock Text="{Binding ModeGlyph}"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Button>

            <Grid>
                <ScrollViewer HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding AppGroups}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="36"
                                        Height="36"
                                        Margin="0,0,14,0"
                                        Command="{Binding DataContext.OpenGroupMenuCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}">
                                    <Button.ToolTip>
                                        <StackPanel MaxWidth="420">
                                            <TextBlock Text="{Binding TooltipHeader}"
                                                       FontWeight="SemiBold"
                                                       Margin="0,0,0,4" />
                                            <TextBlock Text="{Binding TooltipBody}"
                                                       TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Button.ToolTip>
                                    <Image Source="{Binding Icon}"
                                           Width="20"
                                           Height="20"
                                           Stretch="Uniform" />
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <ResizeGrip HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="2"
                            Width="14"
                            Height="14"
                            Foreground="#99FFFFFF" />
            </Grid>
        </DockPanel>
    </Border>
</Window>

